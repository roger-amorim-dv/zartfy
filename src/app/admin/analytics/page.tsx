"use client";
import { useEffect,useMemo,useState } from "react";
import Link from "next/link";
import { ArrowLeft,BarChart3,Eye,ExternalLink,MessageCircle,MousePointerClick,RotateCcw } from "lucide-react";
import { ANALYTICS_KEY,AnalyticsData,readAnalytics } from "@/components/analytics";
export default function AnalyticsPage(){const[authorized,setAuthorized]=useState<boolean|null>(null);const[data,setData]=useState<AnalyticsData|null>(null);useEffect(()=>{const auth=sessionStorage.getItem("zartfy-admin")==="yes";setAuthorized(auth);if(auth)setData(readAnalytics())},[]);const totalViews=useMemo(()=>Object.values(data?.pageViews||{}).reduce((sum,value)=>sum+value,0),[data]);const totalContacts=useMemo(()=>Object.values(data?.events||{}).reduce((sum,value)=>sum+value,0),[data]);if(authorized===null)return null;if(!authorized)return <main className="analytics-locked"><BarChart3/><h1>Analytics is protected</h1><p>Sign in through the admin area to see your visitor summary.</p><Link className="button coral" href="/admin">Go to admin</Link></main>;const reset=()=>{localStorage.removeItem(ANALYTICS_KEY);setData(readAnalytics())};return <main className="analytics-page"><header><Link href="/admin"><ArrowLeft/>Back to catalogue</Link><Link href="/" target="_blank">View website <ExternalLink/></Link></header><section className="analytics-title"><div><p className="kicker">First-party prototype</p><h1>Site analytics</h1><p>Private, cookie-free activity recorded in this browser.</p></div><button onClick={reset}><RotateCcw/>Reset analytics</button></section><section className="metric-grid"><article><Eye/><span>Total page views</span><strong>{totalViews}</strong></article><article><MousePointerClick/><span>Contact actions</span><strong>{totalContacts}</strong></article><article><MessageCircle/><span>WhatsApp clicks</span><strong>{data?.events.contact_whatsapp||0}</strong></article></section><section className="analytics-tables"><article><h2>Pages</h2>{Object.entries(data?.pageViews||{}).sort((a,b)=>b[1]-a[1]).map(([page,views])=><div key={page}><span>{page==="/"?"Homepage":page}</span><strong>{views}</strong></div>)}{!totalViews&&<p>No page views recorded yet.</p>}</article><article><h2>Contact channels</h2>{[["WhatsApp",data?.events.contact_whatsapp||0],["Email",data?.events.contact_email||0],["Instagram",data?.events.contact_instagram||0]].map(([label,value])=><div key={label}><span>{label}</span><strong>{value}</strong></div>)}</article></section><p className="analytics-note">This prototype stores analytics only in the current browser. Connect a production analytics provider before launch for shared, multi-device reporting.</p></main>}
